import LibraryPage from "../library/LibraryPage";

export default function CausesPage() {
    return (
        <LibraryPage
            title="Causes"
            endpoint="/settings/causes"
        />
    )
}